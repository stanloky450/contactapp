{
  "name": "contactkeeper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd server && npm install  && cd ../clients && npm install && npm run build",
    "dev": "cd server && npm install  && cd ../clients && npm install ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
